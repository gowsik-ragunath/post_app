<div class="container">
	<div class="row">
		<div class="col-md-offset-4">
			
			<%= form_for @post do |p| %>
				<p>
					<%= p.text_field :title %>
				</p>
				<p>
					<%= p.text_area :body %>
				</p>
				<p> <%= p.submit %> </p>
			<% end %>
		</div>
	</div>
</div>