<div class="container">
	<div class="row">
		<div class="col-md-6">
			<ul class="post-list">
				<% @post.each do |f| %>
					<%= link_to f.title, topic_post_path(f.topic_id,f), class: "list-body " %>
					<li class="list-body"><%= f.body %></li>
				<% end %>
			</ul>
		</div>
		<div class="co-md-6"></div>
	</div>
</div>
			
